import { createStyles } from '../../../utils/createStyles';

export const styles = createStyles({
  base: {
    textDecoration: 'none',
    alignItems: 'center',
    textAlign: 'center',
    py: 1,
  },
  button: {
    textDecoration: 'none',
    alignItems: 'center',
    textAlign: 'center',
    py: 1,
  },
});
