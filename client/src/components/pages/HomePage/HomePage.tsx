import { Box } from '@mui/material';

export const HomePage: React.FC = () => {
  //take logged in state from context in functional component

  return (
    <Box>
      This is where the homepage will be displayed. Want to find the best match
      for your sports needs? Login/Register (For logged in users Find clubs and
      coaches)
    </Box>
  );
};
